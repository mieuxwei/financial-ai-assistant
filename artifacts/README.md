# Artifacts

Generated market snapshots, research outputs, and model artifacts are ignored by default. Commit only small, reviewed, non-private documentation artifacts when explicitly appropriate. Any experiment report that references a local snapshot should record its SHA-256 checksum.
